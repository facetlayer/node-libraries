{
  "name": "@facetlayer/domain-search-tool",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "main": "dist/main.js",
  "bin": {
    "domains-available": "dist/main.js"
  },
  "scripts": {
    "build": "node build.mts build",
    "test": "vitest --passWithNoTests",
    "prepublishOnly": "node build.mts build && node build.mts validate",
    "local:install": "pnpm build && npm i -g ."
  },
  "dependencies": {
    "yargs": "17.7.2"
  },
  "devDependencies": {
    "@facetlayer/build-config-nodejs": "workspace:*",
    "@types/node": "^22.0.0",
    "@types/yargs": "^17.0.33",
    "typescript": "^5.0.0",
    "vitest": "^3.0.6"
  }
}
