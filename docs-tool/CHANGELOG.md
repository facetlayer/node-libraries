# 0.3.0
 - Split the importable library half (`DocFilesHelper`, `parseFrontmatter`, types)
   into the new `@facetlayer/docs-helper` package. `docs-tool` now depends on it
   and re-exports it for backward compatibility, while focusing on the standalone
   `docs` CLI.

# 0.2.0
 - Add a step to search global NPM installs

# 0.1.3
 - Add docs/ClaudeMdSetup.md

# 0.1.2
 - Fixing the console output

# 0.1.0
 - Initial release
