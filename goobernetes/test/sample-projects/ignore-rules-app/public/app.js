// Ignore Rules App client-side JavaScript
console.log('Ignore Rules App loaded');

document.addEventListener('DOMContentLoaded', () => {
    console.log('DOM ready - Ignore Rules App initialized');
});
