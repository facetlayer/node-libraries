# 0.1.0
 - Initial public release.
 - Session metrics calculation (message counts, tool calls, token usage)
 - Permission check detection and tracking
 - Confidence heuristics with pattern matching
 - CLI with `analyze` and `list` commands
 - Programmatic API for integration
