{
  "name": "@facetlayer/subprocess",
  "version": "1.1.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "test": "vitest run --testTimeout=30000",
    "fix": "prettier \"**/*.{ts,tsx}\" --write",
    "dev": "tsc-watch --onSuccess \"node ./dist/cli/startCli.js\"",
    "build": "tsc -p .",
    "prepublishOnly": "pnpm run build"
  },
  "files": [
    "src",
    "dist",
    "tsconfig.json",
    "README.md"
  ],
  "dependencies": {
    "@facetlayer/parse-stdout-lines": "^1.0.0",
    "@facetlayer/streams": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "22.13.0",
    "typescript": "5.7.3",
    "vitest": "3.2.4"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/facetlayer/node-libraries"
  }
}
